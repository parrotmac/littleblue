endpoint: http://localhost:4466
datamodel: datamodel.prisma

generate:
  - generator: go-client
    output: ./pkg/prisma/

hooks:
  post-deploy:
    - prisma generate
    - go run scripts/gqlgen/gqlgen.go
