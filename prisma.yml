endpoint: http://localhost:4466
datamodel: ./schema/datamodel.prisma

generate:
  - generator: graphql-schema
    output: ./schema/generated.graphql
  - generator: go-client
    output: ./pkg/internal/client/prisma/

hooks:
  post-deploy:
    - prisma generate
    - go run scripts/gqlgen/gqlgen.go
